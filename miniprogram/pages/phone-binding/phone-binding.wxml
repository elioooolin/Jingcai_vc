<!-- pages/phone-binding/phone-binding.wxml -->
<view class="phone-binding-container">
  <!-- 头部说明 -->
  <view class="header-section">
    <view class="logo-wrapper">
      <view class="logo">
        <text class="logo-text">爱睦</text>
      </view>
    </view>
    
    <view class="brand-info">
      <text class="brand-title">爱睦 Love Moon</text>
      <text class="brand-slogan">轻松呵护，用心滋养</text>
    </view>
    
    <view class="binding-info">
      <text class="binding-title">绑定手机号</text>
      <text class="binding-desc">请输入您的手机号完成账户绑定</text>
    </view>
  </view>

  <!-- 手机号输入区域 -->
  <view class="input-section">
    <view class="input-wrapper">
      <t-input
        type="number"
        placeholder="请输入手机号"
        value="{{phone}}"
        bind:change="onPhoneChange"
        maxlength="11"
        class="phone-input"
      />
    </view>
    
    <t-button
      theme="primary"
      size="large"
      block
      loading="{{bindingLoading}}"
      disabled="{{!isPhoneValid || bindingLoading}}"
      bind:tap="handleBindPhone"
      class="bind-button"
    >
      <text wx:if="{{!bindingLoading}}">确认绑定</text>
      <text wx:else>绑定中...</text>
    </t-button>
  </view>

  <!-- 提示信息 -->
  <view class="tip-section">
    <view class="tip-card">
      <text class="tip-title">温馨提示</text>
      <text class="tip-content">
        • 请输入您在爱睦月子中心登记的手机号
        • 如果提示手机号未注册，请联系工作人员
        • 每个手机号只能绑定一个微信账号
      </text>
    </view>
  </view>
</view>

<!-- 错误提示弹窗 -->
<t-dialog
  visible="{{errorDialogVisible}}"
  title="{{errorDialogTitle}}"
  content="{{errorDialogContent}}"
  confirm-btn="我知道了"
  bind:confirm="closeErrorDialog"
  bind:close="closeErrorDialog"
/>

<!-- 联系客服弹窗 -->
<t-dialog
  visible="{{contactDialogVisible}}"
  title="联系客服"
  content="您的手机号未在系统中注册，请联系工作人员为您添加信息。"
  confirm-btn="我知道了"
  bind:confirm="closeContactDialog"
  bind:close="closeContactDialog"
/>
